import Charts
import SwiftUI

struct ChartUtil {
    private static let delta = 1_000_000

    /*
    static func yAxisMarks() -> AxisMarks<Content> {
        AxisMarks(values: .stride(by: Double(delta))) {
            let value = $0.as(Int.self)!
            AxisGridLine()
            AxisTick()
            AxisValueLabel {
                Text(value == 0 ? "" : "\(value / delta)M")
            }
        }
    }
    */
}
